<Window
    x:Class="ProcessingSplashSample.ProcessingSplash"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProcessingSplashSample"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="ProcessingSplash"
    Width="300"
    Height="130"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    WindowStyle="SingleBorderWindow"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="30" />
        </Grid.RowDefinitions>
        <ProgressBar
            x:Name="progressbar"
            Width="250"
            Height="20"
            Margin="10,15,10,5"
            HorizontalAlignment="Stretch"
            IsIndeterminate="True" VerticalAlignment="Center" />
        <Label
            x:Name="label_status"
            Grid.Row="1"
            HorizontalAlignment="Center"
            Content="{Binding}" />
        <Button Click="ButtonCancelOnClick" Content="キャンセル" HorizontalAlignment="Left" Margin="22,16.667,0,-5.333" Grid.Row="1" VerticalAlignment="Top" Width="75"/>
    </Grid>
</Window>